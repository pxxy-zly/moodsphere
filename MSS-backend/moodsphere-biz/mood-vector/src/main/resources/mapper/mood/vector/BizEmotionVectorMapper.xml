<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moodsphere.mood.vector.mapper.BizEmotionVectorMapper">

    <resultMap id="BizEmotionVectorResultMap" type="BizEmotionVector">
        <id property="id" column="id" />
        <result property="recordId" column="record_id" />
        <result property="valence" column="valence" />
        <result property="arousal" column="arousal" />
        <result property="anxiety" column="anxiety" />
        <result property="calmness" column="calmness" />
        <result property="loneliness" column="loneliness" />
        <result property="fatigue" column="fatigue" />
        <result property="anger" column="anger" />
        <result property="hope" column="hope" />
        <result property="confidence" column="confidence" />
        <result property="dimensionJson" column="dimension_json" />
        <result property="confidenceScore" column="confidence_score" />
        <result property="createTime" column="create_time" />
        <result property="createBy" column="create_by" />
        <result property="updateTime" column="update_time" />
        <result property="updateBy" column="update_by" />
        <result property="delFlag" column="del_flag" />
        <result property="remark" column="remark" />
    </resultMap>

    <sql id="selectBizEmotionVectorColumns">
        select id, record_id, valence, arousal, anxiety, calmness, loneliness, fatigue, anger, hope, confidence, dimension_json,
               confidence_score, create_time, create_by, update_time, update_by, del_flag, remark
        from biz_emotion_vector
    </sql>

    <select id="selectByRecordId" resultMap="BizEmotionVectorResultMap">
        <include refid="selectBizEmotionVectorColumns" />
        where record_id = #{recordId}
          and del_flag = 0
        limit 1
    </select>

    <insert id="upsertBizEmotionVector" parameterType="BizEmotionVector">
        insert into biz_emotion_vector (
            record_id, valence, arousal, anxiety, calmness, loneliness, fatigue, anger, hope, confidence, dimension_json,
            confidence_score, create_by, create_time, update_by, update_time, del_flag, remark
        ) values (
            #{recordId}, #{valence}, #{arousal}, #{anxiety}, #{calmness}, #{loneliness}, #{fatigue}, #{anger}, #{hope},
            #{confidence}, #{dimensionJson}, #{confidenceScore}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime},
            #{delFlag}, #{remark}
        )
        on duplicate key update
            valence = values(valence),
            arousal = values(arousal),
            anxiety = values(anxiety),
            calmness = values(calmness),
            loneliness = values(loneliness),
            fatigue = values(fatigue),
            anger = values(anger),
            hope = values(hope),
            confidence = values(confidence),
            dimension_json = values(dimension_json),
            confidence_score = values(confidence_score),
            update_by = values(update_by),
            update_time = values(update_time),
            del_flag = 0,
            remark = values(remark)
    </insert>

</mapper>

